trigger:
  branches:
    include:
      - pdr
      - des
  paths:
    include:
      - apps/mobile/*

pr:
  branches:
    include:
      - pdr
      - des
  paths:
    include:
      - apps/mobile/*

pool:
  vmImage: 'ubuntu-latest'

jobs:
  - job: lint_and_typecheck
    displayName: 'Lint and Typecheck'
    steps:
      - checkout: self

      - task: NodeTool@0
        inputs:
          versionSpec: '24.x'
        displayName: 'Setup Node'

      - task: Cache@2
        inputs:
          key: 'npm | "$(Agent.OS)" | package-lock.json'
          restoreKeys: |
            npm | "$(Agent.OS)"
          path: $(Pipeline.Workspace)/.npm
        displayName: 'Cache npm packages'

      - script: npm ci --cache $(Pipeline.Workspace)/.npm
        displayName: 'Install dependencies'

      - script: npm run lint --workspace=mobile
        displayName: 'Run lint'

      - script: npm run typecheck --workspace=mobile
        displayName: 'Run typecheck'